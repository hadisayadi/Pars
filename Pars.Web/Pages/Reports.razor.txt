@page "/reports"
@inject ApiClient Api
@attribute [Authorize(Roles = "Admin,Manager")]

<h1>📈 گزارش‌ها</h1>

<div class="report-actions">
    <button class="btn-primary" @onclick="ExportAttendance">📥 خروجی اکسل حضور و غیاب</button>
</div>

@code {
    private async Task ExportAttendance()
    {
        var year = DateTime.Now.Year;
        var month = DateTime.Now.Month;
        // Download file
        var url = $"api/reports/attendance/export?year={year}&month={month}";
        // In Blazor WebAssembly, use JSInterop for download
    }
}